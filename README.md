# dbquery

`dbquery` is a small OCaml project that implements a domain-specific query language for CSV-backed tables.

## Quick Start

```sh
dune build
dune test
dune exec dbquery -- data/test1.dbq
```
